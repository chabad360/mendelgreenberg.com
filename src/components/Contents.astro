<div class="content">
    <slot />
</div>

<style>
    .content {
    	width: var(--content-width);
    	margin: 2rem;
    	text-align: justify;

    	font-family: var(--font-serif);
      position: relative;
      font-size: 13pt;
    }

    @media (max-width: 1200px) {
      .content {
        margin-top: 0;
      }
    }

    .content :global(h1,h2,h3,h4,h5,h6) {
        font-family: var(--font-sans);
        font-weight: 400;
        text-align: left;
    }
    .content :global(h1) {
        font-size: 24pt;
    }

    .content :global(p:has(> img):not(:has(> :not(img)))) {
      display: flex;
    }

    .content :global(p:has(> img):not(:has(> :not(img))) img) {
      margin: 0.5rem;
      border-radius: 1rem;
      border: 1px solid var(--color);
    }

    .content :global(p:has(> img):not(:has(> :not(img))) img:first-of-type) {
      margin-left: 0;
    }

    .content :global(p:has(> img):not(:has(> :not(img))) img:last-of-type) {
      margin-right: 0;
    }
</style>
